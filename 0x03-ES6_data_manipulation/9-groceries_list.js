export default function groceriesList() {
  const gloceries = new Map();

  gloceries.set('Apples', 10);
  gloceries.set('Tomatoes', 10);
  gloceries.set('Pasta', 1);
  gloceries.set('Rice', 1);
  gloceries.set('Banana', 5);

  return gloceries;
}
